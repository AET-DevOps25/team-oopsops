apiVersion: v1
kind: ConfigMap
metadata:
  name: oopsops-keycloak-realm
  labels:
    app.kubernetes.io/name: keycloak
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
data:
  test-realm.json: |-
{{ .Files.Get "realms/test-realm.json" | indent 4 }}
