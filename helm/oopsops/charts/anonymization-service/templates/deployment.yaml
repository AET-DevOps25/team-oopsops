apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "anonymization-service.fullname" . }}
  labels:
    {{- include "anonymization-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: anonymization-service
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: anonymization-service
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: anonymization-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.targetPort }}
          env:
            {{- toYaml .Values.env | nindent 12 }}